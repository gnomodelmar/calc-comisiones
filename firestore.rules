rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /appData/paymentMethods {
      // Allow read/write for now, in a real app this should check for authentication
      allow read: if true;
      allow write: if true;
    }
  }
}
